# Docker Compose para Produção
# Acesso via Nginx na porta 80 (único ponto de entrada)

services:
  # ===================================
  # Database - TimescaleDB
  # ===================================
  db:
    image: timescale/timescaledb:latest-pg14
    container_name: agro_db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-mango_farm_monitor}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-mango_farm_monitor}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - agro_network

  # ===================================
  # Backend - FastAPI
  # ===================================
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: agro_api
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-mango_farm_monitor}
      SECRET_KEY: ${SECRET_KEY:-change-me-in-production}
      DEBUG: "false"
      ENVIRONMENT: production
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost,http://localhost:80,http://localhost:8080}
    depends_on:
      db:
        condition: service_healthy
    command: >
      sh -c "
        echo '⏳ Executando migrações...' &&
        alembic upgrade head &&
        echo '🚀 Iniciando servidor...' &&
        uvicorn app.main:app --host 0.0.0.0 --port 8000
      "
    restart: unless-stopped
    networks:
      - agro_network

  # ===================================
  # Frontend - React (nginx)
  # ===================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: /api #${VITE_API_URL:-http://localhost:8000/api}
    container_name: agro_frontend
    depends_on:
      - api
    restart: unless-stopped
    networks:
      - agro_network

  # ===================================
  # Nginx - Proxy Reverso (único ponto de entrada)
  # ===================================
  nginx:
    image: nginx:alpine
    container_name: agro_nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.prod.conf:/etc/nginx/conf.d/default.conf:ro
      - ./data/certbot/conf:/etc/letsencrypt 
      - ./data/certbot/www:/var/www/certbot   
    depends_on:
      - api
      - frontend
    restart: unless-stopped
    networks:
      - agro_network

  # ===================================
  # Certbot - SSL
  # ===================================
  certbot:
    image: certbot/certbot
    container_name: agro_certbot
    volumes:
      - ./data/certbot/conf:/etc/letsencrypt
      - ./data/certbot/www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done'"
    networks:
      - agro_network

  # ===================================
  # MQTT Bridge - Conecta MQTT ao Database
  # ===================================
  mqtt-bridge:
    build:
      context: ./mqtt-bridge
      dockerfile: Dockerfile
    container_name: mqtt_to_db_bridge
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-mango_farm_monitor}
      MQTT_BROKER: ${MQTT_BROKER:-10.0.0.4}
      MQTT_PORT: ${MQTT_PORT:-1883}
      MQTT_TOPIC: ${MQTT_TOPIC:-application/+/device/+/event/up}
      CA_CERT_PATH: ${CA_CERT_PATH:-/app/certs/ca.pem}
      CLIENT_CERT_PATH: ${CLIENT_CERT_PATH:-/app/certs/client-csr.pem}
      CLIENT_KEY_PATH: ${CLIENT_KEY_PATH:-/app/certs/client-csr-key.pem}
    volumes:
      - ./mqtt-bridge/certs:/app/certs:ro
    depends_on:
      db:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - agro_network

volumes:
  postgres_data:

networks:
  agro_network:
    driver: bridge
